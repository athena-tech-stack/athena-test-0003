{
  "name": "metric_line_chart",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/bundle.js --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
